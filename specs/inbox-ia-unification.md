# Inbox IA Unification — moved

The canonical spec now lives with the rest of the Spec Kit artifacts:

**`specs/feature/inbox-ia-unification/spec.md`**

(Alongside `plan.md`, `research.md`, `data-model.md`, `contracts/`, `quickstart.md`.)

The original input draft is preserved in git history at commit `12c9c56`.
