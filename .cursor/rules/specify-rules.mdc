---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---
# unify-memorial-mason-main Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-06-04

## Active Technologies
- TypeScript 5.x, React 18 + Vite, React Router v6, TanStack React Query, Tailwind, shadcn/ui, Supabase JS client (002-multi-org-tenancy)
- PostgreSQL (Supabase), RLS-enabled tables (002-multi-org-tenancy)
- TypeScript 5.x (Vite React app), Deno (Supabase Edge Functions) + React 18, React Router v6, TanStack React Query, Tailwind, shadcn/ui, recharts, Supabase JS client (001-admin-sentry-monitor)
- Gmail REST API, `@supabase/supabase-js`, inbox thread/message backfills, `gmail-sync-now` INBOX+SENT (003-gmail-sent-inbox-sync)
- TypeScript 5.x (Vite + React 18); PostgreSQL (Supabase migrations) + `@supabase/supabase-js`, TanStack React Query, React Router v6, Tailwind + shadcn-style UI under `src/shared/components/ui` (004-org-member-roles)
- PostgreSQL — `public.organizations`, `public.organization_members`; helpers `public.user_is_admin_of_org`, `public.user_is_member_of_org` (004-org-member-roles)
- TypeScript 5.x (frontend), SQL/PLpgSQL (Supabase migrations/RPCs) + React 18, React Router, TanStack Query, Supabase JS client, shadcn/ui dialog/button primitives (005-delete-org-bulk-inbox)
- PostgreSQL (Supabase, RLS-enabled multi-organization tables) (005-delete-org-bulk-inbox)
- TypeScript 5.x (frontend), SQL/PLpgSQL (Supabase migration/RPC) + React 18, React Router v6, TanStack React Query, Supabase JS client, shadcn UI primitives (007-inquiries-pipeline)
- PostgreSQL (Supabase, RLS-enabled multi-org schema) (007-inquiries-pipeline)
- [e.g., Python 3.11, Swift 5.9, Rust 1.75 or NEEDS CLARIFICATION] + [e.g., FastAPI, UIKit, LLVM or NEEDS CLARIFICATION] (009-ghl-inbox-readonly)
- [if applicable, e.g., PostgreSQL, CoreData, files or N/A] (009-ghl-inbox-readonly)
- TypeScript 5.x (Vite React 18); Deno (Supabase Edge Functions) + `@supabase/supabase-js`, TanStack React Query, React Router v6, Tailwind + shadcn/gardens UI (009-ghl-inbox-readonly)
- PostgreSQL — new `public.ghl_connections` only; no GHL message tables (009-ghl-inbox-readonly)
- TypeScript 5.x (Vite React 18); Deno (Supabase Edge Functions) + `@supabase/supabase-js`, TanStack React Query, React Router v6, Tailwind + shadcn UI (011-ghl-inbox-outbound)
- PostgreSQL — `ghl_connections.outbound_enabled`, `ghl_send_idempotency` (metadata only); GHL read-through preserved (011-ghl-inbox-outbound)
- TypeScript 5.x (Vite React 18); Deno (Supabase Edge Functions) + `npm:stripe@14.21.0`, `@stripe/react-stripe-js` (012-per-org-stripe)
- PostgreSQL — `organization_stripe_config` (dual test/live encrypted credentials), `invoices.stripe_credential_mode`; RPCs `get_stripe_secret_key`, `get_stripe_webhook_secret`; Edge `stripe-org-config`, `stripe-get-publishable-key`; org-scoped `stripe-webhook?organization_id=` (012-per-org-stripe)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test; npm run lint

## Code Style

TypeScript 5.x (Vite React app), Deno (Supabase Edge Functions): Follow standard conventions

## Recent Changes
- 012-per-org-stripe: Per-org dual Stripe credential sets (encrypted); org-scoped webhooks; path-specific paid-state; platform-admin `stripe-org-config`; dynamic publishable key per invoice
- 011-ghl-inbox-outbound: Outbound send via `ghl-send-message` Edge Function; `useGhlSendMessage` hook; idempotency table; per-org `outbound_enabled` flag
- 010-ghl-multi-org: Per-org encrypted PIT on `ghl_connections`; multi-org credential refactor
- 009-ghl-inbox-readonly: GHL Inbox read-through module; `ghl-fetch`, `ghl-mark-read`, `ghl-webhook`


<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
